version: '3'
services:
  api:
    build: 
      context: ../
      dockerfile: ./Dockerfile
    user: node
    ports:
      - 3001:3001
  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017
    volumes:
      - ../gestion-data:/data/db/